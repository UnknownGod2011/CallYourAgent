import { notFound, redirect } from "next/navigation"; import { AppShell } from "@/components/app-shell"; import { AgentForm } from "@/components/agent-form"; import { AgentConnection } from "@/components/agent-connection"; import { requireUser } from "@/lib/supabase/server";
export default async function EditAgent({params}:{params:Promise<{id:string}>}){const{id}=await params;const{supabase,user}=await requireUser();if(!user)redirect("/login");const{data:agent}=await supabase.from("agents").select("id,name,description,system_prompt,status").eq("id",id).single();if(!agent)notFound();return <AppShell><section className="page-heading compact"><div><p className="eyebrow">EDIT AGENT</p><h1>{agent.name}</h1><p>Update how this agent introduces itself and handles calls.</p></div></section><AgentForm agent={agent}/><AgentConnection agentId={agent.id} agentName={agent.name}/></AppShell>}
